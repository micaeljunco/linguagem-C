#include <stdio.h>

int main() {
    int valorInteiro;

    printf("Digite um número:\n");
    scanf("%d", &valorInteiro);     // Atribui o valor que o usuario digitar para a variavel
    printf("O número digitado foi: %d\n", valorInteiro);

    return 0;
}
